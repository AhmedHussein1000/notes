import 'package:flutter/material.dart';
import 'package:notes/features/notes/presentation/screens/widgets/add_note_widgets/add_note_body_bloc_consumer.dart';

class AddNote extends StatelessWidget {
  const AddNote({super.key});

  @override
  Widget build(BuildContext context) {
    return const AddNoteBodyBlocConsumer();
  }
}
